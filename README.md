## Usage - Local
Run
```console
streamlit run main.py
```

## Usage - Docker
Run
```console
# Build a local docker image
docker build -t mlimage .
# Run the image
docker run -p 8080:8080 mlimage
```
